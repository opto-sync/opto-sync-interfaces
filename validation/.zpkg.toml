[package]
org = "opto-sync"
name = "opto-sync-public-validation-contracts"
version = "0.1.0"
description = "Public peer validation authorities for opto-sync"
license = "MIT"

[targets.json-schema]
dir = "."
adapter = "none"

[targets.typespec]
dir = "typespec"
adapter = "none"
