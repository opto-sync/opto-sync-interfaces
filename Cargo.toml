[package]
name = "opto-sync-interfaces"
version = "0.1.0"
edition = "2021"
rust-version = "1.88"
license = "MIT"
description = "Transport-neutral Opto Sync protocol and lifecycle types"
repository = "https://github.com/opto-sync/opto-sync-interfaces"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
serde_json = "1"

[lints.rust]
unsafe_code = "forbid"
